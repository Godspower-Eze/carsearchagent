# carsearchagent - A Real-time Web Scrapper

A real-time web scrapper that scraps a site then sends the data. It also has a background scrapper that runs every minute then emails the data to an address
